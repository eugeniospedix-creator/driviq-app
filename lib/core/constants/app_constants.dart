import 'package:flutter/animation.dart';

abstract final class AppConstants {
  static const appName = 'Driviq';
  static const appTagline = 'Vehicle Intelligence';
  static const seedVersion = 2;
  static const defaultAnimationMs = 320;
  static const premiumCurve = Curves.easeOutCubic;
}
